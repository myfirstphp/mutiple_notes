CREATE TABLE `ts_ws_order_after` (
  `id` int(11) unsigned NOT NULL AUTO_INCREMENT COMMENT 'ID',
  `order_id` int(11) NOT NULL COMMENT '订单号',
  `user_id` int(11) NOT NULL COMMENT '用户id',
  `drugstore_branch_id` int(11) NOT NULL COMMENT '药店id',
  `provider_id` int(11) NOT NULL COMMENT '供应商id，这里是指配送商',
  `wholesale_id` int(11) NOT NULL COMMENT '活动id',
  `drug_id` int(11) NOT NULL COMMENT '药品id',
  `drug_name` varchar(64) DEFAULT '' COMMENT '商品名称',
  `unit` varchar(12) DEFAULT '' COMMENT '单位	Not Null	中文药品单位，如盒、包、箱等',
  `amount` int(11) NOT NULL COMMENT '申请售后的药品数量',
  `after_type` tinyint(4) NOT NULL DEFAULT '0' COMMENT '售后类型: 0-商品 1-运费 2-发票 3-资质 4-用户申请整单退款',
  `reason` tinyint(4) NOT NULL DEFAULT '0' COMMENT '申请售后原因参考ts_sys_dict  AFTER_REASON,且需要和ts_wholesale_refund的refund_reason一致\r\n,另外，后台的处理动作有字典项AFTER_OP',
  `confirm_reason` tinyint(4) NOT NULL DEFAULT '-1' COMMENT '申请售后原因参考ts_sys_dict  AFTER_REASON,且需要和ts_wholesale_refund的refund_reason一致',
  `status` tinyint(4) NOT NULL DEFAULT '0' COMMENT '最新状态参考ts_sys_dict AFTER_STATUS，另外有两个字典需要同步修改：0-等待商家确认 1-等待退货 2-已申请退款(退款中) 3-退款完成 4-线下退款(商家退款) 5-已处理换货 6-申请被退回 7未申请（申请撤回） 8-用户已退回货物',
  `refund_total_cost` decimal(10,2) NOT NULL DEFAULT '0.00' COMMENT '退款总金额',
  `descpt` varchar(1000) CHARACTER SET utf8mb4 DEFAULT '' COMMENT '问题描述',
  `logistics_id` int(11) NOT NULL DEFAULT '0' COMMENT 'ts_express_map物流公司在ysb的内部编码,原因是发货少货时填写',
  `logistics_no` varchar(32) NOT NULL DEFAULT '' COMMENT '运单号,原因是发货少货时填写',
  `img1` varchar(200) DEFAULT '' COMMENT '图片1',
  `img2` varchar(200) DEFAULT '' COMMENT '图片2',
  `img3` varchar(200) DEFAULT '' COMMENT '图片3',
  `ctime` int(11) NOT NULL DEFAULT '0' COMMENT '创建时间',
  `mtime` int(11) NOT NULL DEFAULT '0' COMMENT '最后修改时间',
  `rsp_time` int(11) NOT NULL DEFAULT '0' COMMENT '商家第一次回答时间，目前只有after_type=4填写',
  `refund_id` int(11) NOT NULL DEFAULT '0' COMMENT 'ts_whosale_refund的id,售后单:退款单=n:1',
  `parent_id` int(11) NOT NULL DEFAULT '0' COMMENT '再次申请售后时填写上一次的售后单id',
  `note` text CHARACTER SET utf8mb4 COMMENT '说明信息：例如资质售后的内容',
  `export_times` tinyint(11) DEFAULT '0' COMMENT '售后退款推送到ERP次数 0-未推送 1-已推送',
  `export_times_damage` tinyint(4) NOT NULL DEFAULT '0' COMMENT '配送完成后商品压损客户确认后推送的次数',
  PRIMARY KEY (`id`),
  KEY `order_id` (`order_id`),
  KEY `refund_id` (`refund_id`),
  KEY `ctime_idx` (`ctime`),
  KEY `user_idx` (`user_id`,`status`),
  KEY `provider_id` (`provider_id`,`status`),
  KEY `store_idx` (`drugstore_branch_id`)
) ENGINE=InnoDB AUTO_INCREMENT=2449356 DEFAULT CHARSET=utf8 COMMENT='药采购售后单';