14、<、>&、<&、2>

这三个命令也是管道命令，但它们一般不常用，你只需要知道一下就ok了，当然如果想仔细研究的话，可以自己查一下资料。

<   输入重定向命令，从文件中读入命令输入，而不是从键盘中读入。 

>&  将一个句柄的输出写入到另一个句柄的输入中。
 
<&  刚好和>&相反，从一个句柄读取输入并将其写入到另一个句柄输出中。

2>  将命令的错误输出重定向指定文件(先清空)

2>> 将命令的错误输出重定向指定文件(追加到行尾)

&>  将命令的正常输出和错误输出重定向指定文件